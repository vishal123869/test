/**
 * Continuous risk monitoring engine (v4.8).
 *
 * Persists scan history, tracks risk trends over time,
 * and detects risk regressions and spikes.
 */
import type { Finding, RiskHistoryEntry, ScanReport } from "./types.js";
/**
 * Load risk history from persistent storage.
 */
export declare function loadRiskHistory(dir: string): RiskHistoryEntry[];
/**
 * Save current scan result to risk history.
 */
export declare function saveRiskHistory(dir: string, report: ScanReport): void;
/**
 * Analyze risk trend from history.
 */
export declare function analyzeRiskTrend(history: RiskHistoryEntry[], currentScore: number): Finding[];
/**
 * Determine overall risk trend direction.
 */
export declare function getRiskTrend(history: RiskHistoryEntry[]): "increasing" | "stable" | "decreasing";
//# sourceMappingURL=continuous-monitor.d.ts.map