/**
 * Solana C2 wallet monitor
 *
 * Monitors Solana wallet addresses for transactions containing memo instructions.
 * GlassWorm and similar campaigns encode C2 URLs as Solana transaction memos,
 * making the blockchain an uncensorable command-and-control channel.
 */
import type { SolanaMonitorOptions, WatchlistConfig, WatchlistEntry, WatchlistAlert } from "./types.js";
/** Test-only hook: replace the sleep implementation used between RPC retries. */
export declare function __setSleepForTesting(fn: ((ms: number) => Promise<void>) | null): void;
interface TransactionDetail {
    signature: string;
    blockTime: number | null;
    memos: string[];
}
/**
 * Monitor a Solana wallet for C2 memo transactions.
 * Runs continuously until stopped.
 */
export declare function monitorWallet(options: SolanaMonitorOptions, onAlert: (alert: C2Alert) => void): Promise<void>;
/**
 * One-shot check: get recent transactions and check for memos.
 */
export declare function checkWallet(address: string, limit?: number): Promise<TransactionDetail[]>;
/**
 * Alert structure for detected C2 communications.
 */
export interface C2Alert {
    timestamp: string;
    wallet: string;
    signature: string;
    memo: string;
    decodedUrls: string[];
    blockTime: number | null;
}
/**
 * Load the watchlist from disk. Returns an empty list when the file is missing.
 */
export declare function loadWatchlist(): WatchlistConfig;
/**
 * Persist the watchlist to disk.
 */
export declare function saveWatchlist(config: WatchlistConfig): void;
/**
 * Add a wallet to the watchlist.
 */
export declare function addToWatchlist(address: string, name: string): WatchlistEntry;
/**
 * Remove a wallet from the watchlist.
 */
export declare function removeFromWatchlist(address: string): void;
/**
 * Return all watchlist entries.
 */
export declare function listWatchlist(): WatchlistEntry[];
/**
 * Monitor all wallets on the watchlist. Polls each wallet and fires alerts
 * for new memo transactions. Optionally forwards alerts to a webhook.
 */
export declare function monitorWatchlist(options: {
    interval: number;
    limit: number;
    webhookUrl?: string;
}, onAlert: (alert: WatchlistAlert) => void): Promise<void>;
export declare function formatAlert(alert: C2Alert): string;
export {};
//# sourceMappingURL=solana-monitor.d.ts.map