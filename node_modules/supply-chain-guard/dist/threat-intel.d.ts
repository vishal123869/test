/**
 * Threat intelligence integration (v4.5).
 *
 * Loads external IOC feeds (JSON), merges with local blocklist,
 * and provides confidence-scored IOC matching with decay.
 */
import type { Finding } from "./types.js";
export interface FeedIOC {
    type: "domain" | "ip" | "url" | "hash" | "package";
    value: string;
    severity: "critical" | "high" | "medium";
    confidence: number;
    family?: string;
    campaign?: string;
    source?: string;
    firstSeen?: string;
    lastSeen?: string;
}
/**
 * Load and merge IOC feeds. Starts with bundled feed, merges remote if available.
 */
export declare function loadThreatIntel(cacheDir?: string, remoteFeedUrl?: string): FeedIOC[];
/**
 * Update remote threat feed and cache locally.
 */
export declare function updateThreatFeed(feedUrl: string, cacheDir?: string): Promise<{
    added: number;
    total: number;
}>;
export declare function checkThreatIntel(content: string, relativePath: string, feed: FeedIOC[]): Finding[];
//# sourceMappingURL=threat-intel.d.ts.map