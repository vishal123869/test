"use strict";
/**
 * Core file scanner
 *
 * Scans local directories and GitHub repos for supply-chain malware indicators.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.scan = scan;
const fs = __importStar(require("node:fs"));
const path = __importStar(require("node:path"));
const node_child_process_1 = require("node:child_process");
const types_js_1 = require("./types.js");
const patterns_js_1 = require("./patterns.js");
const lockfile_checker_js_1 = require("./lockfile-checker.js");
const github_actions_scanner_js_1 = require("./github-actions-scanner.js");
const dockerfile_scanner_js_1 = require("./dockerfile-scanner.js");
const config_scanner_js_1 = require("./config-scanner.js");
const git_scanner_js_1 = require("./git-scanner.js");
const entropy_js_1 = require("./entropy.js");
const cargo_scanner_js_1 = require("./cargo-scanner.js");
const go_scanner_js_1 = require("./go-scanner.js");
const ioc_blocklist_js_1 = require("./ioc-blocklist.js");
const github_trust_scanner_js_1 = require("./github-trust-scanner.js");
const patterns_js_2 = require("./patterns.js");
const install_hook_scanner_js_1 = require("./install-hook-scanner.js");
const dependency_risk_analyzer_js_1 = require("./dependency-risk-analyzer.js");
const correlation_engine_js_1 = require("./correlation-engine.js");
const trust_breakdown_js_1 = require("./trust-breakdown.js");
const policy_engine_js_1 = require("./policy-engine.js");
const trust_signals_js_1 = require("./trust-signals.js");
const threat_intel_js_1 = require("./threat-intel.js");
const risk_engine_js_1 = require("./risk-engine.js");
const diff_scanner_js_1 = require("./diff-scanner.js");
const remediation_engine_js_1 = require("./remediation-engine.js");
const playbooks_js_1 = require("./playbooks.js");
const attack_graph_js_1 = require("./attack-graph.js");
const active_validation_js_1 = require("./active-validation.js");
const workflow_modeler_js_1 = require("./workflow-modeler.js");
const continuous_monitor_js_1 = require("./continuous-monitor.js");
const triage_engine_js_1 = require("./triage-engine.js");
const risk_forecast_js_1 = require("./risk-forecast.js");
const metrics_js_1 = require("./metrics.js");
const patterns_js_3 = require("./patterns.js");
const sbom_generator_js_1 = require("./sbom-generator.js");
const slsa_verifier_js_1 = require("./slsa-verifier.js");
const dependency_confusion_js_1 = require("./dependency-confusion.js");
const TOOL_VERSION = "5.2.0";
/** Regex matching the scanner's own source files (pattern/IOC/threat-intel definitions). */
const SCANNER_SOURCE_FILE = /(?:patterns|scanner|playbooks|correlation-engine|ioc-blocklist|threat-intel|remediation-engine|secret-simulator|workflow-modeler|config-scanner|install-hook-scanner|github-trust-scanner|dependency-confusion|attack-graph|reporter|active-validation)\.(ts|js)$/;
/** Detect test / spec / fixture / mock files. Shared constant (was duplicated inline). */
const TEST_FILE_REGEX = /[._-](test|spec|mock|fixture|stub|fake)\.|__tests__|\/tests?\/|conftest\.py/i;
/**
 * Scan a local directory or GitHub repo for malware indicators.
 */
async function scan(options) {
    const startTime = Date.now();
    const target = options.target;
    let scanDir = target;
    let scanType = "directory";
    let tempDir = null;
    // If target is a GitHub URL, clone it
    if (target.startsWith("https://github.com/")) {
        scanType = "github";
        tempDir = fs.mkdtempSync(path.join("/tmp", "scg-"));
        try {
            (0, node_child_process_1.execSync)(`git clone --depth 1 "${target}" "${tempDir}/repo"`, {
                stdio: "pipe",
            });
        }
        catch {
            throw new Error(`Failed to clone repository: ${target}`);
        }
        scanDir = path.join(tempDir, "repo");
    }
    // Validate directory exists
    if (!fs.existsSync(scanDir)) {
        throw new Error(`Target directory does not exist: ${scanDir}`);
    }
    const stat = fs.statSync(scanDir);
    if (!stat.isDirectory()) {
        throw new Error(`Target is not a directory: ${scanDir}`);
    }
    // Collect files (v4.5: diff mode filters to changed files only)
    let allFiles = collectFiles(scanDir, options.maxDepth ?? 20);
    if (options.sinceCommit && fs.existsSync(path.join(scanDir, ".git"))) {
        const changedFiles = new Set((0, diff_scanner_js_1.getChangedFiles)(scanDir, options.sinceCommit));
        if (changedFiles.size > 0) {
            allFiles = allFiles.filter((f) => changedFiles.has(f));
        }
    }
    // v4.5: Load threat intelligence feed
    const threatFeed = (0, threat_intel_js_1.loadThreatIntel)();
    let findings = [];
    // Scan each file
    let filesScanned = 0;
    for (const filePath of allFiles) {
        const ext = path.extname(filePath).toLowerCase();
        const basename = path.basename(filePath);
        const relativePath = path.relative(scanDir, filePath).replace(/\\/g, "/");
        // Check suspicious file names
        checkSuspiciousFileName(basename, relativePath, findings);
        // Check for unexpected binary/native addon files (T-007)
        if (patterns_js_1.BINARY_EXTENSIONS.has(ext)) {
            checkBinaryFile(relativePath, findings);
        }
        // Scan Docker/Config files inline (v4.0)
        if ((0, dockerfile_scanner_js_1.isDockerFile)(basename) || (0, config_scanner_js_1.isConfigFile)(basename)) {
            try {
                const content = fs.readFileSync(filePath, "utf-8");
                if ((0, dockerfile_scanner_js_1.isDockerFile)(basename)) {
                    findings.push(...(0, dockerfile_scanner_js_1.scanDockerFile)(content, relativePath));
                }
                if ((0, config_scanner_js_1.isConfigFile)(basename)) {
                    findings.push(...(0, config_scanner_js_1.scanConfigFile)(content, relativePath));
                }
            }
            catch { /* skip */ }
        }
        // Only scan content of known file types
        if (!patterns_js_1.SCANNABLE_EXTENSIONS.has(ext))
            continue;
        // Skip large files
        const fileStat = fs.statSync(filePath);
        if (fileStat.size > patterns_js_1.MAX_FILE_SIZE)
            continue;
        filesScanned++;
        try {
            const content = fs.readFileSync(filePath, "utf-8");
            // Check file content patterns
            checkFilePatterns(content, relativePath, findings);
            // Check build tool configs for plugin risks (v4.0)
            if (patterns_js_1.BUILD_CONFIG_FILES.has(basename)) {
                checkBuildToolPatterns(content, relativePath, findings);
            }
            // Check workspace/monorepo patterns (v4.0)
            if (basename === "package.json" && relativePath === "package.json") {
                checkMonorepoPatterns(content, relativePath, findings);
            }
            // Check beacon and miner patterns (T-008)
            checkBeaconMinerPatterns(content, relativePath, findings);
            // Entropy analysis for obfuscated payloads (v4.0)
            const entropyFindings = (0, entropy_js_1.analyzeEntropy)(content, relativePath);
            findings.push(...entropyFindings);
            // IOC blocklist + threat-intel checks (skip scanner source/test files — they contain IOC definitions)
            const normForIOC = relativePath.replace(/\\/g, "/");
            const isIOCExcluded = SCANNER_SOURCE_FILE.test(normForIOC) || TEST_FILE_REGEX.test(normForIOC);
            if (!isIOCExcluded) {
                const iocFindings = (0, ioc_blocklist_js_1.checkIOCBlocklist)(content, relativePath);
                findings.push(...iocFindings);
                const tiFindings = (0, threat_intel_js_1.checkThreatIntel)(content, relativePath, threatFeed);
                findings.push(...tiFindings);
            }
            // README / doc-file lure pattern scanning (v4.1, scope expanded v5.2.20)
            // Covers README, CHANGELOG, CONTRIBUTING, DESCRIPTION, release-notes -
            // matches LURE_PATTERNS' onlyFilePattern. Previously only `readme*`
            // files were routed here, but the LURE patterns themselves are scoped
            // to all of these. After v5.2.20 removed LURE_PATTERNS from the
            // general checkFilePatterns sweep (dedupe fix), scanReadmeLures became
            // the sole entry point so it now covers the full doc-file family.
            if (/^(?:readme|changelog|contributing|description|release[-_]notes)/i.test(basename)) {
                const lureFindings = (0, github_trust_scanner_js_1.scanReadmeLures)(content, relativePath);
                findings.push(...lureFindings);
            }
            // Check package.json specifically (skip test fixture directories)
            const normPkgPath = relativePath.replace(/\\/g, "/");
            if (basename === "package.json" && !TEST_FILE_REGEX.test(normPkgPath)) {
                checkPackageJson(content, relativePath, findings);
                // Check for binary download patterns in install scripts (T-007)
                checkBinaryDownloadScripts(content, relativePath, findings);
                // Check for known-bad package versions (v4.1)
                checkKnownBadVersions(content, relativePath, findings);
                // Deep install hook analysis (v4.2)
                const hookScripts = (0, install_hook_scanner_js_1.extractInstallScripts)(content);
                if (hookScripts) {
                    const hookFindings = (0, install_hook_scanner_js_1.analyzeInstallHooks)(hookScripts, relativePath);
                    findings.push(...hookFindings);
                }
                // Dependency risk analysis (v4.2)
                try {
                    const pkg = JSON.parse(content);
                    const allDeps = {
                        ...pkg.dependencies,
                        ...pkg.devDependencies,
                    };
                    if (Object.keys(allDeps).length > 0) {
                        const depFindings = (0, dependency_risk_analyzer_js_1.analyzeDependencyRisks)(allDeps, relativePath);
                        findings.push(...depFindings);
                    }
                }
                catch { /* not valid JSON */ }
            }
            // Check package-lock.json for known-bad versions (v4.1)
            if (basename === "package-lock.json") {
                checkLockfileBadVersions(content, relativePath, findings);
            }
        }
        catch {
            // Skip files that can't be read (binary, permissions, etc.)
        }
    }
    // Check git commit dates if it's a git repo
    if (fs.existsSync(path.join(scanDir, ".git"))) {
        checkGitDateAnomalies(scanDir, findings);
    }
    // Check lockfile integrity (T-006)
    const lockfileFindings = (0, lockfile_checker_js_1.checkLockfile)(scanDir);
    findings.push(...lockfileFindings);
    // Check GitHub Actions workflows (#9)
    const ghaFindings = (0, github_actions_scanner_js_1.scanGitHubActionsWorkflows)(scanDir);
    findings.push(...ghaFindings);
    // v4.9: SLSA provenance verification
    findings.push(...(0, slsa_verifier_js_1.verifySLSA)(scanDir));
    // v4.9: PyPI dependency confusion (if requirements.txt / pyproject.toml present)
    try {
        const pypiConfusion = await (0, dependency_confusion_js_1.scanPypiDependencyConfusion)(scanDir);
        findings.push(...pypiConfusion);
    }
    catch { /* skip if offline */ }
    // GitHub trust signal analysis (v4.1)
    if (scanType === "github") {
        const parsed = (0, github_trust_scanner_js_1.parseGitHubUrl)(target);
        if (parsed) {
            const trustFindings = (0, github_trust_scanner_js_1.analyzeGitHubTrust)(parsed.owner, parsed.repo);
            findings.push(...trustFindings);
        }
    }
    // Check Dockerfile / container configs (v4.0)
    const dockerFindings = (0, dockerfile_scanner_js_1.scanDockerFiles)(scanDir);
    findings.push(...dockerFindings);
    // Check package manager config files (v4.0)
    const configFindings = (0, config_scanner_js_1.scanConfigFiles)(scanDir);
    findings.push(...configFindings);
    // Check git hooks and submodules (v4.0)
    if (fs.existsSync(path.join(scanDir, ".git"))) {
        const gitSecFindings = (0, git_scanner_js_1.scanGitSecurity)(scanDir);
        findings.push(...gitSecFindings);
    }
    // Check Cargo/Rust files (v4.0)
    if (fs.existsSync(path.join(scanDir, "Cargo.toml"))) {
        const cargoFindings = (0, cargo_scanner_js_1.scanCargoFiles)(scanDir);
        findings.push(...cargoFindings);
    }
    // Check Go module files (v4.0)
    if (fs.existsSync(path.join(scanDir, "go.mod"))) {
        const goFindings = (0, go_scanner_js_1.scanGoFiles)(scanDir);
        findings.push(...goFindings);
    }
    // v4.7: Workflow execution modeling
    const wfFindings = (0, workflow_modeler_js_1.modelWorkflows)(scanDir);
    findings.push(...wfFindings);
    // v4.4: Detect positive trust signals (only for GitHub repo scans)
    if (scanType === "github") {
        const trustSignals = (0, trust_signals_js_1.detectTrustSignals)(scanDir);
        findings.push(...trustSignals);
    }
    // v4.7: Active validation (assign confidence tiers, rationale, evidence)
    (0, active_validation_js_1.validateFindings)(findings);
    // v4.2: Correlation engine — link findings into incidents
    const correlation = (0, correlation_engine_js_1.correlateFindings)(findings);
    // v4.2: Trust breakdown (for directory/github scans with package.json)
    const hasLockfile = fs.existsSync(path.join(scanDir, "package-lock.json"));
    const trustBreakdown = (0, trust_breakdown_js_1.calculateTrustBreakdown)(findings, target, hasLockfile);
    // v4.8: Continuous risk monitoring
    const riskHistory = (0, continuous_monitor_js_1.loadRiskHistory)(scanDir);
    const trendFindings = (0, continuous_monitor_js_1.analyzeRiskTrend)(riskHistory, calculateScore(findings));
    findings.push(...trendFindings);
    const forecastFindings = (0, risk_forecast_js_1.forecastRisk)(riskHistory, calculateScore(findings));
    findings.push(...forecastFindings);
    // v4.8: Triage governance checks
    const triageDecisions = (0, triage_engine_js_1.loadTriageDecisions)(scanDir);
    const govFindings = (0, triage_engine_js_1.checkTriageGovernance)(findings, triageDecisions);
    findings.push(...govFindings);
    // v4.4: Apply policy config (if present)
    let suppressedCount = 0;
    const policy = (0, policy_engine_js_1.loadPolicyConfig)(scanDir);
    if (policy) {
        const policyResult = (0, policy_engine_js_1.applyPolicy)(findings, policy);
        findings = policyResult.findings;
        suppressedCount += policyResult.suppressedCount;
    }
    // v4.4: Apply baseline (if configured)
    const baselineFile = options.baselineFile ?? policy?.baseline?.file;
    if (baselineFile) {
        const baselinePath = path.isAbsolute(baselineFile)
            ? baselineFile
            : path.join(scanDir, baselineFile);
        const baselineResult = (0, policy_engine_js_1.applyBaseline)(findings, baselinePath);
        findings = baselineResult.findings;
        suppressedCount += baselineResult.suppressedCount;
    }
    // Filter by severity and excluded rules
    const filteredFindings = filterFindings(findings, options);
    // Calculate summary and score (with correlation risk boost)
    const summary = calculateSummary(allFiles.length, filesScanned, filteredFindings);
    const baseScore = calculateScore(filteredFindings);
    const score = Math.min(100, baseScore + correlation.riskBoost);
    const riskLevel = getRiskLevel(score);
    const recommendations = generateRecommendations(filteredFindings);
    // v4.8: Calculate metrics and save history
    const metrics = (0, metrics_js_1.calculateMetrics)(filteredFindings, riskHistory, triageDecisions);
    // Save risk history for trend tracking (skip temp dirs)
    if (!tempDir) {
        try {
            (0, continuous_monitor_js_1.saveRiskHistory)(scanDir, { timestamp: new Date().toISOString(), score, findings: filteredFindings, summary, riskLevel, recommendations, target, scanType, tool: `supply-chain-guard v${TOOL_VERSION}`, durationMs: Date.now() - startTime });
        }
        catch { /* skip */ }
    }
    // Cleanup temp directory
    if (tempDir) {
        fs.rmSync(tempDir, { recursive: true, force: true });
    }
    return {
        tool: `supply-chain-guard v${TOOL_VERSION}`,
        timestamp: new Date().toISOString(),
        target,
        scanType,
        durationMs: Date.now() - startTime,
        findings: filteredFindings,
        summary,
        score,
        riskLevel,
        recommendations,
        incidents: correlation.incidents.length > 0 ? correlation.incidents : undefined,
        trustBreakdown,
        suppressedCount: suppressedCount > 0 ? suppressedCount : undefined,
        riskDimensions: (0, risk_engine_js_1.calculateRiskDimensions)(filteredFindings),
        remediations: (0, remediation_engine_js_1.generateRemediations)(filteredFindings),
        fixSuggestions: (0, remediation_engine_js_1.generateFixSuggestions)(filteredFindings),
        playbooks: correlation.incidents.length > 0
            ? (0, playbooks_js_1.generatePlaybooks)(correlation.incidents)
            : undefined,
        attackGraph: filteredFindings.length > 0
            ? (0, attack_graph_js_1.buildAttackGraph)(filteredFindings, target)
            : undefined,
        riskHistory: riskHistory.length > 0 ? riskHistory : undefined,
        metrics,
        // v4.9: CycloneDX 1.6 SBOM from actual dependency inventory
        sbomDocument: (0, sbom_generator_js_1.generateSbomDocument)(scanDir, filteredFindings),
        // v4.9: SLSA provenance level
        slsaLevel: (0, slsa_verifier_js_1.getSLSALevel)(scanDir),
    };
}
/**
 * Recursively collect all files in a directory.
 */
function collectFiles(dir, maxDepth, depth = 0) {
    if (depth > maxDepth)
        return [];
    const files = [];
    let entries;
    try {
        entries = fs.readdirSync(dir, { withFileTypes: true });
    }
    catch {
        return files;
    }
    for (const entry of entries) {
        const fullPath = path.join(dir, entry.name);
        // Skip common non-relevant directories
        if (entry.isDirectory()) {
            if (entry.name === "node_modules" ||
                entry.name === ".git" ||
                entry.name === "dist" ||
                entry.name === "build" ||
                entry.name === ".next" ||
                entry.name === "__pycache__" ||
                entry.name === ".venv" ||
                entry.name === "venv" ||
                entry.name === ".claude") {
                continue;
            }
            files.push(...collectFiles(fullPath, maxDepth, depth + 1));
        }
        else if (entry.isFile()) {
            files.push(fullPath);
        }
    }
    return files;
}
/**
 * Check if a filename matches known suspicious patterns.
 */
function checkSuspiciousFileName(basename, relativePath, findings) {
    for (const suspicious of patterns_js_1.SUSPICIOUS_FILES) {
        const regex = new RegExp(suspicious.pattern);
        if (regex.test(basename)) {
            findings.push({
                rule: suspicious.rule,
                description: suspicious.description,
                severity: suspicious.severity,
                file: relativePath,
                recommendation: `Inspect ${relativePath} manually. This filename is commonly associated with malware campaigns.`,
            });
        }
    }
}
/**
 * Scan file content against known malicious patterns.
 */
function checkFilePatterns(content, relativePath, findings) {
    const lines = content.split("\n");
    // Note: LURE_PATTERNS deliberately excluded here. README-style files are
    // already covered by scanReadmeLures() in github-trust-scanner.ts (called
    // earlier in scanDirectory). Including them in this loop produced duplicate
    // findings for every README hit (same rule+file+line+match, different
    // recommendation text). v5.2.20 dedupe fix.
    const allPatterns = [
        ...patterns_js_1.FILE_PATTERNS,
        ...patterns_js_1.CAMPAIGN_PATTERNS,
        ...patterns_js_1.CAMPAIGN_PATTERNS_V2,
        ...patterns_js_1.OBFUSCATION_PATTERNS_V2,
        ...patterns_js_1.IAC_PATTERNS,
        ...patterns_js_2.INFOSTEALER_PATTERNS,
        ...patterns_js_2.PROMPT_INJECTION_PATTERNS,
        ...patterns_js_2.C2_EXTENDED_PATTERNS,
        ...patterns_js_2.SECRETS_PATTERNS,
        ...patterns_js_3.OBFUSCATION_V3_PATTERNS,
        ...patterns_js_3.PROVENANCE_PATTERNS,
    ];
    const fileExt = path.extname(relativePath).toLowerCase();
    // Normalise path separators for cross-platform regex matching
    const normalizedPath = relativePath.replace(/\\/g, "/");
    const isTestFile = TEST_FILE_REGEX.test(normalizedPath);
    for (const pattern of allPatterns) {
        // Respect onlyExtensions restriction (e.g. SVG_SCRIPT_INJECTION → .svg only)
        if (pattern.onlyExtensions && !pattern.onlyExtensions.includes(fileExt))
            continue;
        // Respect onlyFilePattern (e.g. README_LURE → README/docs files only)
        if (pattern.onlyFilePattern && !pattern.onlyFilePattern.test(normalizedPath))
            continue;
        // Respect notFilePattern (e.g. skip .min.js for framework-heavy patterns)
        if (pattern.notFilePattern && pattern.notFilePattern.test(normalizedPath))
            continue;
        // Skip test/spec/fixture files for patterns marked notTestFile
        if (pattern.notTestFile && isTestFile)
            continue;
        const regex = new RegExp(pattern.pattern, "g");
        for (let i = 0; i < lines.length; i++) {
            const line = lines[i];
            const match = regex.exec(line);
            if (match) {
                findings.push({
                    rule: pattern.rule,
                    description: pattern.description,
                    severity: pattern.severity,
                    file: relativePath,
                    line: i + 1,
                    match: truncateMatch(match[0]),
                    recommendation: getRecommendation(pattern.rule),
                });
                // Reset regex lastIndex for next line
                regex.lastIndex = 0;
            }
        }
    }
}
/**
 * Check package.json for suspicious install scripts.
 */
function checkPackageJson(content, relativePath, findings) {
    let pkg;
    try {
        pkg = JSON.parse(content);
    }
    catch {
        return;
    }
    const scripts = pkg.scripts;
    if (!scripts)
        return;
    const dangerousHooks = ["preinstall", "postinstall", "preuninstall", "postuninstall"];
    for (const hook of dangerousHooks) {
        const script = scripts[hook];
        if (!script)
            continue;
        // Check against suspicious script patterns
        for (const pattern of patterns_js_1.SUSPICIOUS_SCRIPTS) {
            const regex = new RegExp(pattern.pattern, "i");
            if (regex.test(script)) {
                findings.push({
                    rule: pattern.rule,
                    description: `${hook}: ${pattern.description}`,
                    severity: pattern.severity,
                    file: relativePath,
                    match: truncateMatch(`${hook}: ${script}`),
                    recommendation: `Review the ${hook} script in ${relativePath}. Suspicious install scripts are a primary vector for supply-chain attacks.`,
                });
            }
        }
        // Flag any non-trivial postinstall/preinstall
        if ((hook === "postinstall" || hook === "preinstall") &&
            script.length > 50 &&
            !findings.some((f) => f.file === relativePath &&
                f.rule.startsWith("SCRIPT_"))) {
            findings.push({
                rule: "COMPLEX_INSTALL_SCRIPT",
                description: `Complex ${hook} script detected (${script.length} chars). Long install scripts warrant manual review.`,
                severity: "low",
                file: relativePath,
                match: truncateMatch(`${hook}: ${script}`),
                recommendation: `Review the ${hook} script to ensure it only performs expected build/setup operations.`,
            });
        }
    }
}
/**
 * Check for git commit date anomalies (committer date much newer than author date).
 */
function checkGitDateAnomalies(dir, findings) {
    try {
        const log = (0, node_child_process_1.execSync)(`git -C "${dir}" log --format="%H|%aI|%cI" -20 2>/dev/null`, { encoding: "utf-8", stdio: ["pipe", "pipe", "pipe"] });
        const lines = log.trim().split("\n").filter(Boolean);
        for (const line of lines) {
            const [hash, authorDate, committerDate] = line.split("|");
            if (!hash || !authorDate || !committerDate)
                continue;
            const authorTime = new Date(authorDate).getTime();
            const committerTime = new Date(committerDate).getTime();
            const diffHours = (committerTime - authorTime) / (1000 * 60 * 60);
            // Flag if committer date is more than 30 days newer than author date
            if (diffHours > 30 * 24) {
                findings.push({
                    rule: "GIT_DATE_ANOMALY",
                    description: `Git commit date anomaly: committer date is ${Math.round(diffHours / 24)} days newer than author date. This can indicate repository history manipulation.`,
                    severity: "medium",
                    match: `commit ${hash?.substring(0, 8)}: authored ${authorDate}, committed ${committerDate}`,
                    recommendation: "Investigate the commit history. Large gaps between author and committer dates may indicate a hijacked or manipulated repository.",
                });
            }
        }
    }
    catch {
        // Not a git repo or git not available
    }
}
/**
 * Check for unexpected binary/native addon files (T-007).
 */
function checkBinaryFile(relativePath, findings) {
    // Check if the binary belongs to a known native package
    const parts = relativePath.split(/[/\\]/);
    const isKnownNative = parts.some((part) => patterns_js_1.KNOWN_NATIVE_PACKAGES.has(part));
    if (isKnownNative) {
        findings.push({
            rule: "BINARY_KNOWN_NATIVE",
            description: `Binary file "${relativePath}" belongs to a known native addon package.`,
            severity: "info",
            file: relativePath,
            recommendation: "This is expected for known native addon packages. Verify the package is intentionally included.",
        });
    }
    else {
        findings.push({
            rule: "BINARY_UNEXPECTED",
            description: `Unexpected binary/native file detected: "${relativePath}". Binary files in npm packages are unusual unless the package is a known native addon.`,
            severity: "high",
            file: relativePath,
            recommendation: "Inspect this binary file. Legitimate npm packages rarely include precompiled binaries unless they are known native addon packages (e.g., sharp, better-sqlite3).",
        });
    }
}
/**
 * Check package.json install scripts for binary download patterns (T-007).
 */
function checkBinaryDownloadScripts(content, relativePath, findings) {
    let pkg;
    try {
        pkg = JSON.parse(content);
    }
    catch {
        return;
    }
    const scripts = pkg.scripts;
    if (!scripts)
        return;
    const pkgName = pkg.name ?? "";
    const isKnownNative = patterns_js_1.KNOWN_NATIVE_PACKAGES.has(pkgName);
    const installHooks = ["preinstall", "postinstall", "install"];
    for (const hook of installHooks) {
        const script = scripts[hook];
        if (!script)
            continue;
        for (const pattern of patterns_js_1.BINARY_DOWNLOAD_PATTERNS) {
            const regex = new RegExp(pattern.pattern, "i");
            if (regex.test(script)) {
                findings.push({
                    rule: pattern.rule,
                    description: `${hook}: ${pattern.description}${isKnownNative ? " (known native package)" : ""}`,
                    severity: isKnownNative ? "info" : pattern.severity,
                    file: relativePath,
                    match: truncateMatch(`${hook}: ${script}`),
                    recommendation: isKnownNative
                        ? `This is expected for ${pkgName}. Native packages commonly download prebuilt binaries.`
                        : `Review the ${hook} script. Binary downloads in install scripts can be a supply-chain attack vector.`,
                });
            }
        }
    }
}
/**
 * Scan file content for beacon and crypto miner patterns (T-008).
 */
function checkBeaconMinerPatterns(content, relativePath, findings) {
    const lines = content.split("\n");
    const normalizedPath = relativePath.replace(/\\/g, "/");
    const isTestFile = TEST_FILE_REGEX.test(normalizedPath);
    for (const pattern of patterns_js_1.BEACON_MINER_PATTERNS) {
        // Apply context-aware file filters (same guards as checkFilePatterns)
        if (pattern.onlyFilePattern && !pattern.onlyFilePattern.test(normalizedPath))
            continue;
        if (pattern.notFilePattern && pattern.notFilePattern.test(normalizedPath))
            continue;
        if (pattern.notTestFile && isTestFile)
            continue;
        const regex = new RegExp(pattern.pattern, "gi");
        for (let i = 0; i < lines.length; i++) {
            const line = lines[i] ?? "";
            const match = regex.exec(line);
            if (match) {
                findings.push({
                    rule: pattern.rule,
                    description: pattern.description,
                    severity: pattern.severity,
                    file: relativePath,
                    line: i + 1,
                    match: truncateMatch(match[0]),
                    recommendation: getRecommendation(pattern.rule),
                });
                regex.lastIndex = 0;
            }
        }
    }
    // Multi-line protestware check: locale/timezone on one line, destructive on nearby lines
    checkMultiLineProtestware(content, relativePath, findings);
}
/**
 * Check for protestware patterns spanning multiple lines.
 * Looks for locale/timezone checks within 15 lines of destructive operations.
 */
function checkMultiLineProtestware(content, relativePath, findings) {
    // Skip scanner source and test files (contain protestware patterns as definitions)
    const normPath = relativePath.replace(/\\/g, "/");
    if (SCANNER_SOURCE_FILE.test(normPath) || TEST_FILE_REGEX.test(normPath))
        return;
    const lines = content.split("\n");
    const PROXIMITY = 15;
    const localePattern = /(?:locale|timezone|timeZone|Intl\.DateTimeFormat|getTimezone|country_code|country_name)/i;
    const destructivePattern = /(?:fs\.(?:rmSync|unlinkSync|rmdirSync|rmdir|unlink|rm)\s*\(|rimraf\s*\(|process\.exit\s*\(|execSync\s*\(["'`](?:rm|del|format|dd\s))/i;
    const localeLines = [];
    const destructiveLines = [];
    for (let i = 0; i < lines.length; i++) {
        const line = lines[i] ?? "";
        if (localePattern.test(line))
            localeLines.push(i);
        if (destructivePattern.test(line))
            destructiveLines.push(i);
    }
    // Check if any locale line is within PROXIMITY of a destructive line
    for (const localeLine of localeLines) {
        for (const destructiveLine of destructiveLines) {
            if (Math.abs(localeLine - destructiveLine) <= PROXIMITY &&
                localeLine !== destructiveLine) {
                // Avoid duplicating if we already found a single-line match
                const alreadyFound = findings.some((f) => f.rule === "PROTESTWARE_LOCALE_DESTRUCT" &&
                    f.file === relativePath);
                if (!alreadyFound) {
                    findings.push({
                        rule: "PROTESTWARE_PROXIMITY",
                        description: `Locale/timezone check (line ${localeLine + 1}) found near destructive code (line ${destructiveLine + 1}). This proximity pattern is common in protestware.`,
                        severity: "high",
                        file: relativePath,
                        line: localeLine + 1,
                        match: truncateMatch(`locale: "${(lines[localeLine] ?? "").trim()}" ... destruct: "${(lines[destructiveLine] ?? "").trim()}"`),
                        recommendation: "Review the surrounding code. Protestware uses locale/geo checks to selectively destroy data or crash for targeted users.",
                    });
                }
                return; // One finding per file is enough
            }
        }
    }
}
/**
 * Check package.json dependencies for known-bad versions (v4.1).
 */
function checkKnownBadVersions(content, relativePath, findings) {
    let pkg;
    try {
        pkg = JSON.parse(content);
    }
    catch {
        return;
    }
    const allDeps = {
        ...pkg.dependencies,
        ...pkg.devDependencies,
    };
    for (const [name, versionRange] of Object.entries(allDeps)) {
        if (!versionRange)
            continue;
        // Extract exact version from range (e.g., "^1.14.1" → "1.14.1")
        const version = versionRange.replace(/^[\^~>=<]*/, "");
        const finding = (0, ioc_blocklist_js_1.checkBadVersion)(name, version, "npm");
        if (finding) {
            findings.push({ ...finding, file: relativePath });
        }
    }
}
/**
 * Check package-lock.json for known-bad resolved versions (v4.1).
 */
function checkLockfileBadVersions(content, relativePath, findings) {
    let lock;
    try {
        lock = JSON.parse(content);
    }
    catch {
        return;
    }
    // lockfile v2+ uses "packages" key
    const packages = lock.packages;
    if (packages) {
        for (const [pkgPath, entry] of Object.entries(packages)) {
            if (!pkgPath || !entry?.version)
                continue;
            // Extract package name from path (e.g., "node_modules/axios" → "axios")
            const name = pkgPath.replace(/^node_modules\//, "").replace(/^.*node_modules\//, "");
            if (!name)
                continue;
            const finding = (0, ioc_blocklist_js_1.checkBadVersion)(name, entry.version, "npm");
            if (finding) {
                findings.push({ ...finding, file: relativePath });
            }
        }
    }
    // lockfile v1 uses "dependencies" key
    const deps = lock.dependencies;
    if (deps && !packages) {
        for (const [name, entry] of Object.entries(deps)) {
            if (!entry?.version)
                continue;
            const finding = (0, ioc_blocklist_js_1.checkBadVersion)(name, entry.version, "npm");
            if (finding) {
                findings.push({ ...finding, file: relativePath });
            }
        }
    }
}
/**
 * Check build tool config files for suspicious plugin patterns (v4.0).
 */
function checkBuildToolPatterns(content, relativePath, findings) {
    const lines = content.split("\n");
    for (const pattern of patterns_js_1.BUILD_TOOL_PATTERNS) {
        const regex = new RegExp(pattern.pattern, "gi");
        for (let i = 0; i < lines.length; i++) {
            const line = lines[i] ?? "";
            const match = regex.exec(line);
            if (match) {
                findings.push({
                    rule: pattern.rule,
                    description: pattern.description,
                    severity: pattern.severity,
                    file: relativePath,
                    line: i + 1,
                    match: truncateMatch(match[0]),
                    recommendation: getRecommendation(pattern.rule),
                });
                regex.lastIndex = 0;
            }
        }
    }
}
/**
 * Check root package.json for monorepo/workspace risks (v4.0).
 */
function checkMonorepoPatterns(content, relativePath, findings) {
    let pkg;
    try {
        pkg = JSON.parse(content);
    }
    catch {
        return;
    }
    // Only check if it has workspaces
    if (!pkg.workspaces)
        return;
    const lines = content.split("\n");
    for (const pattern of patterns_js_1.MONOREPO_PATTERNS) {
        const regex = new RegExp(pattern.pattern, "gi");
        for (let i = 0; i < lines.length; i++) {
            const line = lines[i] ?? "";
            const match = regex.exec(line);
            if (match) {
                findings.push({
                    rule: pattern.rule,
                    description: pattern.description,
                    severity: pattern.severity,
                    file: relativePath,
                    line: i + 1,
                    match: truncateMatch(match[0]),
                    recommendation: getRecommendation(pattern.rule),
                });
                regex.lastIndex = 0;
            }
        }
    }
}
/**
 * Filter findings based on scan options.
 */
function filterFindings(findings, options) {
    const severityOrder = {
        critical: 4,
        high: 3,
        medium: 2,
        low: 1,
        info: 0,
    };
    let filtered = findings;
    if (options.minSeverity) {
        const minLevel = severityOrder[options.minSeverity] ?? 0;
        filtered = filtered.filter((f) => (severityOrder[f.severity] ?? 0) >= minLevel);
    }
    if (options.excludeRules?.length) {
        const excluded = new Set(options.excludeRules);
        filtered = filtered.filter((f) => !excluded.has(f.rule));
    }
    return filtered;
}
/**
 * Calculate summary statistics.
 */
function calculateSummary(totalFiles, filesScanned, findings) {
    return {
        totalFiles,
        filesScanned,
        critical: findings.filter((f) => f.severity === "critical").length,
        high: findings.filter((f) => f.severity === "high").length,
        medium: findings.filter((f) => f.severity === "medium").length,
        low: findings.filter((f) => f.severity === "low").length,
        info: findings.filter((f) => f.severity === "info").length,
    };
}
/**
 * Calculate overall risk score (0-100).
 * Each unique rule contributes at most once (its highest severity instance),
 * preventing repeated instances of the same moderate rule from dominating the score.
 */
// Meta/governance findings that fire because other findings exist — excluded from
// score to prevent circular inflation (they don't represent independent risk signals).
const SCORE_EXCLUDED_RULES = new Set([
    "CRITICAL_FINDING_NO_OWNER",
    "RISK_STAGNATION_HIGH",
]);
function calculateScore(findings) {
    // Deduplicate by rule — take the highest-severity instance per rule.
    // Skip meta-governance findings that would circularly inflate the score.
    const maxByRule = new Map();
    for (const finding of findings) {
        if (SCORE_EXCLUDED_RULES.has(finding.rule))
            continue;
        const current = maxByRule.get(finding.rule);
        if (!current || types_js_1.SEVERITY_SCORES[finding.severity] > types_js_1.SEVERITY_SCORES[current]) {
            maxByRule.set(finding.rule, finding.severity);
        }
    }
    let score = 0;
    for (const severity of maxByRule.values()) {
        score += types_js_1.SEVERITY_SCORES[severity];
    }
    return Math.min(100, score);
}
/**
 * Derive risk level from score.
 */
function getRiskLevel(score) {
    if (score === 0)
        return "clean";
    if (score <= 10)
        return "low";
    if (score <= 30)
        return "medium";
    if (score <= 60)
        return "high";
    return "critical";
}
/**
 * Generate human-readable recommendations.
 */
function generateRecommendations(findings) {
    const recommendations = [];
    const rules = new Set(findings.map((f) => f.rule));
    if (rules.has("GLASSWORM_MARKER")) {
        recommendations.push("CRITICAL: GlassWorm malware marker detected. Quarantine this code immediately and audit all downstream dependencies.");
    }
    if (rules.has("EVAL_ATOB") || rules.has("EVAL_BUFFER") || rules.has("FUNCTION_ATOB")) {
        recommendations.push("CRITICAL: Encoded code execution detected. This is a strong indicator of malicious obfuscation. Do not run this code.");
    }
    if (rules.has("INVISIBLE_UNICODE")) {
        recommendations.push("Review files with invisible Unicode characters. These can hide malicious code in otherwise normal-looking files.");
    }
    if (rules.has("SOLANA_MAINNET") || rules.has("HELIUS_RPC")) {
        recommendations.push("Solana blockchain references detected. If this project has no legitimate blockchain functionality, this may indicate C2 communication via the Solana blockchain.");
    }
    if (rules.has("SCRIPT_CURL_EXEC") ||
        rules.has("SCRIPT_WGET_EXEC") ||
        rules.has("SCRIPT_NODE_INLINE")) {
        recommendations.push("Dangerous install scripts detected. These scripts download and execute remote code, which is a common supply-chain attack vector.");
    }
    if (rules.has("GIT_DATE_ANOMALY")) {
        recommendations.push("Git commit date anomalies detected. Verify the repository history has not been manipulated.");
    }
    if (rules.has("ENV_EXFILTRATION") || rules.has("DNS_EXFILTRATION")) {
        recommendations.push("Potential data exfiltration patterns detected. Environment variables may be sent to external servers.");
    }
    // Campaign-specific recommendations
    if (rules.has("XZ_GET_CPUID") || rules.has("XZ_LZMA_CRC64") || rules.has("XZ_BUILD_INJECT") || rules.has("XZ_OBFUSCATED_TEST")) {
        recommendations.push("CRITICAL: XZ Utils backdoor indicators detected (CVE-2024-3094). Verify xz/liblzma versions and inspect build scripts for unauthorized modifications.");
    }
    if (rules.has("CODECOV_CURL_BASH") || rules.has("CODECOV_EXFIL")) {
        recommendations.push("Codecov supply-chain attack indicators detected. Avoid piping remote scripts to shell. Use pinned checksums for CI uploader binaries.");
    }
    if (rules.has("SUNBURST_DGA") || rules.has("SUNBURST_ORION_CLASS") || rules.has("SUNBURST_DELAYED_EXEC")) {
        recommendations.push("CRITICAL: SolarWinds SUNBURST indicators detected. Quarantine immediately. Check for DGA domains and unusual delayed execution patterns.");
    }
    if (rules.has("UAPARSER_MINER") || rules.has("UAPARSER_PREINSTALL_DL")) {
        recommendations.push("CRITICAL: ua-parser-js hijack indicators detected. Check for crypto miner binaries and suspicious preinstall script downloads.");
    }
    if (rules.has("COA_RC_SDD_DLL") || rules.has("COA_RC_POSTINSTALL")) {
        recommendations.push("CRITICAL: coa/rc npm hijack indicators detected. Check for sdd.dll references and encoded postinstall payloads. Pin dependency versions.");
    }
    if (rules.has("DPRK_VALIDATE_SDK")) {
        recommendations.push("CRITICAL: DPRK @validate-sdk/v2 indicator detected. April 2026 campaign delivered AI-inserted malicious npm dependencies. Remove the package and audit all AI-suggested dependencies.");
    }
    if (rules.has("LOFYSTEALER_MARKER") || rules.has("LOFYGANG_MINECRAFT_LURE")) {
        recommendations.push("CRITICAL: LofyGang / LofyStealer (GrabBot) indicators detected. April 2026 campaign targets Minecraft players via fake hack tools. Do not run any associated binaries.");
    }
    // Lockfile recommendations (T-006)
    if (rules.has("LOCKFILE_MISSING_INTEGRITY") ||
        rules.has("LOCKFILE_INVALID_INTEGRITY") ||
        rules.has("LOCKFILE_SHORT_INTEGRITY")) {
        recommendations.push("Lockfile integrity issues detected. Run `npm install` with a modern npm version to regenerate integrity hashes.");
    }
    if (rules.has("LOCKFILE_HTTP_RESOLVED")) {
        recommendations.push("Packages resolving over plain HTTP detected. Update resolved URLs to HTTPS to prevent MITM attacks.");
    }
    if (rules.has("LOCKFILE_VERSION_DOWNGRADE")) {
        recommendations.push("Lockfile uses an outdated version. Upgrade with `npm install` using npm 7+ for stronger security guarantees.");
    }
    // Binary detection recommendations (T-007)
    if (rules.has("BINARY_UNEXPECTED")) {
        recommendations.push("Unexpected binary files detected. Inspect them carefully. Legitimate npm packages rarely ship precompiled binaries.");
    }
    if (rules.has("BINARY_DIRECT_DOWNLOAD")) {
        recommendations.push("Install scripts download binary files directly. This is a supply-chain risk. Verify the download source.");
    }
    // Beacon/miner recommendations (T-008)
    if (rules.has("MINER_STRATUM_PROTOCOL") ||
        rules.has("MINER_POOL_DOMAIN") ||
        rules.has("MINER_LIBRARY_REF")) {
        recommendations.push("CRITICAL: Cryptocurrency miner indicators detected. This package likely contains a hidden miner. Do NOT install.");
    }
    if (rules.has("BEACON_INTERVAL_FETCH") || rules.has("BEACON_TIMEOUT_FETCH")) {
        recommendations.push("Network beacon patterns detected. Periodic outbound requests may indicate C2 communication or data exfiltration.");
    }
    if (rules.has("PROTESTWARE_LOCALE_DESTRUCT") ||
        rules.has("PROTESTWARE_GEOIP_DESTRUCT") ||
        rules.has("PROTESTWARE_PROXIMITY")) {
        recommendations.push("CRITICAL: Protestware patterns detected. This code targets users by geographic location with destructive operations.");
    }
    // GitHub Actions workflow recommendations (#9)
    if (rules.has("GHA_CURL_PIPE_EXEC") ||
        rules.has("GHA_WGET_PIPE_EXEC") ||
        rules.has("GHA_CURL_DOWNLOAD_EXEC") ||
        rules.has("GHA_WGET_DOWNLOAD_EXEC")) {
        recommendations.push("CI workflow fetches and executes remote content. Pin scripts by checksum or use pinned GitHub Actions instead.");
    }
    if (rules.has("GHA_SECRET_CURL") ||
        rules.has("GHA_SECRET_WGET") ||
        rules.has("GHA_SECRET_EXFIL_MULTILINE")) {
        recommendations.push("CRITICAL: Secrets may be exfiltrated via network commands in CI workflows. Audit all workflow steps that combine secrets with curl/wget.");
    }
    if (rules.has("GHA_UNPINNED_ACTION")) {
        recommendations.push("Unpinned GitHub Actions detected. Pin actions to commit SHAs to prevent supply-chain attacks via mutable branch references.");
    }
    if (rules.has("GHA_BASE64_PAYLOAD") ||
        rules.has("GHA_BASE64_EXEC")) {
        recommendations.push("Base64 encoded payloads in CI workflows are suspicious. Decode and inspect the content before allowing execution.");
    }
    // Docker recommendations (v4.0)
    if (rules.has("DOCKER_CURL_PIPE")) {
        recommendations.push("CRITICAL: Dockerfile pipes remote content to shell. Download, verify checksum, then execute.");
    }
    if (rules.has("DOCKER_UNPINNED_BASE") || rules.has("DOCKER_NO_TAG")) {
        recommendations.push("Pin Docker base images by digest (FROM image@sha256:...) to ensure reproducible and tamper-proof builds.");
    }
    if (rules.has("DOCKER_SECRETS_BUILD")) {
        recommendations.push("Remove hardcoded secrets from Dockerfile. Use BuildKit secrets or runtime environment variables.");
    }
    // Config file recommendations (v4.0)
    if (rules.has("CONFIG_HTTP_REGISTRY")) {
        recommendations.push("CRITICAL: Package manager uses HTTP registry. Switch to HTTPS to prevent MITM attacks.");
    }
    if (rules.has("CONFIG_AUTH_TOKEN_EXPOSED")) {
        recommendations.push("CRITICAL: Auth tokens found in config files. Rotate tokens immediately and use environment variables.");
    }
    // Git recommendations (v4.0)
    if (rules.has("GIT_HOOK_DOWNLOAD") || rules.has("GIT_HOOK_ENCODED")) {
        recommendations.push("Suspicious git hooks detected. Remove hooks that download or decode content. Use pre-commit frameworks instead.");
    }
    // Build tool recommendations (v4.0)
    if (rules.has("BUILD_ENV_EXFIL")) {
        recommendations.push("CRITICAL: Build config may exfiltrate environment secrets. Audit build tool plugins and configurations.");
    }
    // Campaign recommendations (v4.0)
    if (rules.has("SHAI_HULUD_WORM") || rules.has("SHAI_HULUD_CRED_STEAL")) {
        recommendations.push("CRITICAL: Shai-Hulud worm indicators detected. This self-replicating malware steals npm tokens. Quarantine and rotate all npm credentials.");
    }
    // IaC recommendations (v4.0)
    if (rules.has("IAC_HARDCODED_SECRET")) {
        recommendations.push("CRITICAL: Hardcoded secrets in IaC files. Use secret managers (Vault, AWS Secrets Manager) or encrypted variables.");
    }
    // Entropy recommendations (v4.0)
    if (rules.has("HIGH_ENTROPY_STRING")) {
        recommendations.push("High-entropy strings detected. Decode and inspect these strings for hidden payloads or encoded malware.");
    }
    // Cargo/Go recommendations (v4.0)
    if (rules.has("CARGO_BUILD_RS_EXEC") || rules.has("CARGO_PROC_MACRO_NETWORK")) {
        recommendations.push("Suspicious Rust build.rs or proc-macro code detected. Build scripts and macros run with full privileges at compile time.");
    }
    if (rules.has("GO_INIT_EXEC")) {
        recommendations.push("Go init() functions with command execution detected. init() runs automatically on import.");
    }
    // Infostealer / dead-drop recommendations (v4.1)
    if (rules.has("DEAD_DROP_STEAM") || rules.has("DEAD_DROP_TELEGRAM") || rules.has("DEAD_DROP_PASTEBIN")) {
        recommendations.push("CRITICAL: Dead-drop resolver patterns detected. Infostealers (Vidar, Lumma, RedLine) use Steam/Telegram/Pastebin to retrieve C2 addresses. Quarantine this code.");
    }
    if (rules.has("VIDAR_BROWSER_THEFT") || rules.has("VIDAR_WALLET_THEFT")) {
        recommendations.push("CRITICAL: Infostealer credential/wallet theft patterns detected. This code targets browser data and cryptocurrency wallets.");
    }
    if (rules.has("GHOSTSOCKS_SOCKS5") || rules.has("PROXY_BACKCONNECT")) {
        recommendations.push("CRITICAL: Proxy/backconnect malware detected. GhostSocks turns infected machines into residential proxy nodes for criminal infrastructure.");
    }
    if (rules.has("DROPPER_TEMP_EXEC")) {
        recommendations.push("CRITICAL: Dropper/loader behavior detected — writing and executing files in temporary directories.");
    }
    // IOC blocklist recommendations (v4.1)
    if (rules.has("IOC_KNOWN_C2_DOMAIN") || rules.has("IOC_KNOWN_C2_IP") || rules.has("IOC_KNOWN_DEAD_DROP")) {
        recommendations.push("CRITICAL: Known malicious infrastructure (C2/dead-drop) detected in code. This is a confirmed threat indicator.");
    }
    if (rules.has("IOC_KNOWN_BAD_VERSION")) {
        recommendations.push("CRITICAL: Known compromised package version detected. Remove immediately and upgrade to a clean version.");
    }
    // Lure / fake repo recommendations (v4.1)
    if (rules.has("CAMPAIGN_CLAUDE_LURE") || rules.has("CAMPAIGN_AI_TOOL_LURE")) {
        recommendations.push("CRITICAL: This matches the 2026 fake AI tool malware campaign distributing Vidar/GhostSocks. Do NOT execute any code or binaries.");
    }
    if (rules.has("README_LURE_CRACK") || rules.has("RELEASE_NAME_LURE")) {
        recommendations.push("This repository uses piracy/crack language — a strong indicator of malware distribution. Do not download or use.");
    }
    // GitHub trust recommendations (v4.1)
    if (rules.has("REPO_KNOWN_MALICIOUS_ACCOUNT")) {
        recommendations.push("CRITICAL: This repository belongs to a known malicious GitHub account. Do not use any code from this source.");
    }
    if (rules.has("RELEASE_EXE_ARTIFACT")) {
        recommendations.push("CRITICAL: Executable files in GitHub releases. This is the primary distribution vector for the 2026 fake AI tool campaign.");
    }
    if (recommendations.length === 0 && findings.length > 0) {
        recommendations.push("Review the listed findings and assess whether they represent legitimate functionality or potential threats.");
    }
    if (findings.length === 0) {
        recommendations.push("No malicious indicators detected. The scanned code appears clean.");
    }
    return recommendations;
}
/**
 * Get a recommendation string for a specific rule.
 */
function getRecommendation(rule) {
    const map = {
        GLASSWORM_MARKER: "Quarantine this code immediately. This is a known GlassWorm campaign indicator.",
        INVISIBLE_UNICODE: "Inspect this file in a hex editor. Invisible characters may hide malicious code.",
        EVAL_ATOB: "Do not execute this code. Decode the base64 content to inspect what would be evaluated.",
        EVAL_BUFFER: "Do not execute this code. Inspect the Buffer contents to see the hidden payload.",
        FUNCTION_ATOB: "Do not execute this code. The Function constructor with encoded content is a strong malware indicator.",
        EVAL_HEX: "Do not execute this code. Decode the hex string to inspect the hidden payload.",
        EXEC_ENCODED: "Review what this exec call is decoding and running.",
        SOLANA_MAINNET: "If this project has no blockchain functionality, this reference may indicate C2 communication.",
        HELIUS_RPC: "Helius RPC references in non-blockchain projects are suspicious. Investigate.",
        HEX_ARRAY: "Large hex arrays may contain obfuscated payloads. Decode and inspect.",
        CHARCODE_OBFUSCATION: "String construction from character codes is a common obfuscation technique.",
        ENV_EXFILTRATION: "This pattern combines environment variable access with network requests, which is a data exfiltration indicator.",
        DNS_EXFILTRATION: "DNS-based exfiltration encodes data in DNS queries. This is a covert data theft technique.",
        // Campaign-specific rules
        XZ_GET_CPUID: "This matches the XZ Utils backdoor (CVE-2024-3094). The _get_cpuid function was used to hook into sshd. Verify liblzma provenance.",
        XZ_LZMA_CRC64: "lzma_crc64 was the hijacked symbol in CVE-2024-3094. Ensure your xz/liblzma is from a trusted source.",
        XZ_BUILD_INJECT: "Build system injection matching the XZ Utils attack pattern. Inspect configure.ac and m4 macros for unauthorized changes.",
        XZ_OBFUSCATED_TEST: "Obfuscated test file extraction pattern matching CVE-2024-3094. Check test fixtures for hidden payloads.",
        CODECOV_CURL_BASH: "Piping curl output to bash is inherently risky. Use checksummed binary downloads instead.",
        CODECOV_EXFIL: "Codecov uploader was compromised to exfiltrate CI secrets. Verify uploader integrity and rotate exposed credentials.",
        SUNBURST_DGA: "avsvmcloud.com is the known SUNBURST C2 domain. This is a critical indicator of compromise.",
        SUNBURST_ORION_CLASS: "OrionImprovementBusinessLayer is the SUNBURST backdoor namespace. Quarantine this code immediately.",
        SUNBURST_DELAYED_EXEC: "Long sleep/timeout delays are a SUNBURST evasion technique to bypass sandbox analysis. Investigate the purpose of this delay.",
        UAPARSER_MINER: "This matches the ua-parser-js crypto miner pattern. Check for jsextension binaries and unauthorized downloads.",
        UAPARSER_PREINSTALL_DL: "Preinstall scripts downloading executables match the ua-parser-js hijack pattern. Review and remove.",
        COA_RC_SDD_DLL: "sdd.dll is the payload from the coa/rc npm hijack. This is a critical indicator of compromise.",
        COA_RC_POSTINSTALL: "Encoded postinstall payloads match the coa/rc npm hijack pattern. Pin dependencies and audit install scripts.",
        BEACON_INTERVAL_FETCH: "Periodic network requests (setInterval + fetch) can be C2 beacons. Verify this is legitimate functionality.",
        BEACON_TIMEOUT_FETCH: "Delayed network requests may be beacons with jitter. Investigate the target URL.",
        MINER_STRATUM_PROTOCOL: "Stratum protocol is exclusively used for cryptocurrency mining. This is a strong malware indicator.",
        MINER_POOL_DOMAIN: "Known mining pool domain detected. Do not run this code.",
        MINER_CONFIG_KEYS: "Mining configuration parameters detected. This code may be configuring a cryptocurrency miner.",
        MINER_LIBRARY_REF: "Cryptocurrency miner library referenced. Do not run this code.",
        BEACON_WEBSOCKET_EXTERNAL: "WebSocket to external host detected. Verify this is expected for the package.",
        PROTESTWARE_LOCALE_DESTRUCT: "Protestware detected: locale/geo check combined with destructive operations. Do not run.",
        PROTESTWARE_GEOIP_DESTRUCT: "GeoIP-based protestware detected. Do not run this code.",
        PROTESTWARE_PROXIMITY: "Locale check near destructive code. Review carefully for protestware patterns.",
        BINARY_UNEXPECTED: "Unexpected binary file in package. Inspect with a hex editor or disassembler.",
        BINARY_DIRECT_DOWNLOAD: "Binary download in install script. Verify the download source is trusted.",
        // GitHub Actions rules (#9)
        GHA_CURL_PIPE_EXEC: "Do not pipe remote content directly to a shell in CI. Download, verify checksum, then execute.",
        GHA_WGET_PIPE_EXEC: "Do not pipe remote content directly to a shell in CI. Download, verify checksum, then execute.",
        GHA_SECRET_CURL: "Secrets sent to external URLs via curl may indicate credential exfiltration.",
        GHA_SECRET_WGET: "Secrets sent to external URLs via wget may indicate credential exfiltration.",
        GHA_UNPINNED_ACTION: "Pin GitHub Actions to commit SHAs instead of mutable branch references.",
        GHA_BASE64_PAYLOAD: "Base64 encoded payloads in CI workflows are suspicious. Decode and inspect.",
        GHA_BASE64_EXEC: "Base64 decoded content piped to shell is a common attack vector.",
        // v4.0 rules
        BUILD_PLUGIN_DOWNLOAD: "Build plugin downloads external code. Verify the source is trusted.",
        BUILD_PLUGIN_EXEC: "Build config executes system commands. Ensure this is expected build behavior.",
        BUILD_ENV_EXFIL: "Build config combines env vars with network. This can exfiltrate secrets during build.",
        BUILD_DYNAMIC_REQUIRE: "Dynamic require in build config can load unexpected modules.",
        WORKSPACE_ROOT_POSTINSTALL: "Root postinstall scripts in monorepos affect all workspaces. Audit carefully.",
        WORKSPACE_PRIVATE_PUBLISH: "Non-private workspace with publishConfig may unintentionally be published.",
        SHAI_HULUD_WORM: "CRITICAL: Self-publishing pattern matches the Shai-Hulud npm worm. Quarantine immediately.",
        SHAI_HULUD_CRED_STEAL: "npm credential access detected. This matches the Shai-Hulud worm's token theft pattern.",
        PROTESTWARE_IP_GEO_V2: "IP geolocation combined with destructive ops. Advanced protestware targeting.",
        TEMPLATE_LITERAL_EXEC: "eval with template literals can hide complex expressions. Inspect the template.",
        PROXY_HANDLER_TRAP: "Proxy handler traps can intercept all operations. Review for data interception.",
        IMPORT_EXPRESSION: "Dynamic import() with computed URL can load attacker-controlled modules.",
        WASM_SUSPICIOUS: "WebAssembly loaded from external source. Verify the WASM module is trusted.",
        STEGANOGRAPHY_DECODE: "Base64 decoding of image/font data suggests steganographic payload extraction.",
        SVG_SCRIPT_INJECTION: "SVG with embedded scripts can execute JavaScript. Sanitize SVG files.",
        RTL_OVERRIDE: "RTL override characters can disguise file names and code. Inspect in a hex editor.",
        IAC_INLINE_SCRIPT: "IaC provisioner pipes remote content to shell. Download, verify, then execute.",
        IAC_EXTERNAL_MODULE: "Terraform module from non-standard source. Use registry modules or pin by hash.",
        IAC_HARDCODED_SECRET: "Remove hardcoded secrets. Use Terraform variables with sensitive=true or a secret manager.",
        IAC_REMOTE_EXEC: "remote-exec provisioner runs commands on remote resources. Audit the commands.",
        HIGH_ENTROPY_FILE: "High-entropy file may contain obfuscated or compressed payloads.",
        HIGH_ENTROPY_STRING: "High-entropy string likely contains encoded payload. Decode and inspect.",
        // v4.1 rules
        DEAD_DROP_STEAM: "CRITICAL: Steam profile used as dead-drop resolver for C2. This is a Vidar/Lumma stealer indicator.",
        DEAD_DROP_TELEGRAM: "CRITICAL: Telegram channel used as dead-drop resolver. This is a known infostealer C2 technique.",
        DEAD_DROP_PASTEBIN: "Pastebin URL may be a dead-drop resolver for malware C2 configuration.",
        DEAD_DROP_DNS_TXT: "DNS TXT lookups can be used as covert C2 channels by malware.",
        VIDAR_BROWSER_THEFT: "Browser credential file access matches infostealer behavior. Quarantine this code.",
        VIDAR_WALLET_THEFT: "Crypto wallet file access detected. Infostealers target these paths for fund theft.",
        GHOSTSOCKS_SOCKS5: "SOCKS5 proxy protocol detected. GhostSocks malware creates residential proxy nodes.",
        PROXY_BACKCONNECT: "Backconnect/reverse proxy registration. Infected machines become proxy infrastructure.",
        DROPPER_TEMP_EXEC: "Temp directory write + execute pattern. This is classic dropper/loader behavior.",
        DROPPER_ANTIVM: "Anti-VM/anti-debug checks indicate sandbox evasion. Malware avoids analysis environments.",
        DROPPER_SLEEP_EVASION: "Long sleep before execution evades sandbox time limits. Common in infostealers.",
        README_LURE_LEAKED: "README uses 'leaked' language. Verify project legitimacy before using.",
        README_LURE_CRACK: "README promises cracked/unlocked software. This is almost certainly malware.",
        README_LURE_URGENCY: "Urgency language pressures quick downloads. Classic social engineering.",
        CAMPAIGN_CLAUDE_LURE: "CRITICAL: Claude Code lure matches April 2026 Vidar/GhostSocks campaign.",
        CAMPAIGN_AI_TOOL_LURE: "CRITICAL: Fake AI tool lure matches 2026 multi-brand malware campaign.",
        FAKE_AI_TOOL_LURE: "Suspicious executable naming pattern. Verify file origin and integrity.",
        IOC_KNOWN_C2_DOMAIN: "Known malicious C2 domain. Quarantine immediately.",
        IOC_KNOWN_C2_IP: "Known malicious C2 IP address. Quarantine immediately.",
        IOC_KNOWN_DEAD_DROP: "Known dead-drop resolver URL. This is used to retrieve malware C2 addresses.",
        IOC_KNOWN_MALWARE_HASH: "This hash matches known malware. Do not execute associated files.",
        IOC_KNOWN_MALICIOUS_ACCOUNT: "Reference to known malicious GitHub account. Do not use code from this source.",
        IOC_KNOWN_BAD_VERSION: "This package version is known to contain malware. Remove and upgrade immediately.",
        REPO_KNOWN_MALICIOUS_ACCOUNT: "This GitHub account distributes malware. Do not use.",
        REPO_NEW_ACCOUNT: "New GitHub account. Verify maintainer identity.",
        REPO_RECENT_CREATION: "New repo with many stars. Likely star-farming.",
        REPO_STAR_FORK_RATIO: "Unusual star/fork ratio indicates bot activity.",
        REPO_FEW_CONTRIBUTORS: "Few contributors on popular repo. May be fake.",
        REPO_NO_ISSUES: "Issues disabled or empty on popular repo. Red flag.",
        REPO_SINGLE_COMMIT: "Single-commit repo with stars. Strong malware indicator.",
        RELEASE_EXE_ARTIFACT: "Do NOT download. Executables in GitHub releases are a primary malware vector.",
        RELEASE_7Z_ARCHIVE: "Compressed archives bypass AV detection. Inspect before extracting.",
        RELEASE_SIZE_ANOMALY: "Unusually large release artifact. Verify this is expected.",
        RELEASE_NAME_LURE: "Release name contains piracy/crack language. Almost always malware.",
    };
    return map[rule] ?? "Review this finding manually and assess the risk.";
}
/**
 * Truncate a match string for display.
 */
function truncateMatch(match, maxLen = 120) {
    if (match.length <= maxLen)
        return match;
    return match.substring(0, maxLen) + "...";
}
//# sourceMappingURL=scanner.js.map